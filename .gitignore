node_modules/
package-lock.json

# 含 API key，绝对不进仓库
model.config.json

# 隔离会话的 agentDir，运行时生成
.polish-agent/

# 运行时与本地数据一概不进仓库（仓库自带 sample/wiki）
data/

# 内部施工文档不进这个仓库 —— 它是产品仓库，不是项目工作区。
# 施工台账（执行决议 D-01~）、施工文档、取样数据都归工作区
# `教育实习生项目/代码/施工记录/`，那边才是它们的正经位置。
执行决议.md
docs/施工-*.md
docs/sampling/

