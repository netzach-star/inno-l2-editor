# v2.0 批次一 · 执行决议

> 施工期间由人类拍板、或在实现中不得不做出的决定，逐条记在这里。
> **这份文档是设计文档 `项目设计-V2.0更新.md` 的下位文档**——设计文档说"做什么"，
> 这里记"施工时具体怎么落地、为什么这么落地"。
>
> 格式：每条一个小节，写清 **决议 / 理由 / 影响到哪**。
> 与设计文档冲突时以设计文档为准，除非本文明确记着"人类改判"。

---

## 元信息

| | |
|---|---|
| 基线 | `netzach-star/inno-l2-editor` `origin/main` = `c90232c`（v1.1.0 + 2 个提交） |
| 施工范围 | 设计文档 §16 **批次一全部 8 项**，约 720 行 |
| 工作目录 | `教育实习生项目/代码/inno-l2-editor_v2.0/`（新 clone，v1.0 那份原样封存） |
| 上游沙盒 | `教育实习生项目/代码/inno-agent/`，上游 HEAD = `924ccbc` |
| 开工日期 | 2026-08-07 |

---

## D-01 · 施工范围定为批次一全部 8 项

**决议（人类，2026-08-07）**：这一轮做设计文档 §16 批次一的全部 8 项，不多做也不少做。

| # | 项 | 行数 |
|---|---|---|
| 1 | U-D 既有入口 + 寄存区接线（含三条转发路由） | ~235 |
| 2 | U-J-1 乐观锁 + 原子写 | ~40 |
| 3 | 13.2 抗漂移自检 + 真实全流程兼容测试 | ~135 |
| 4 | U-E 页面管理（搜索 / 删除 / 类型过滤） | ~130 |
| 5 | U-F① 只读反向链接 | ~40 |
| 6 | U-K 收尾（拆对端写入 + 删除清单） | ~25 |
| 7 | U-J-2 frontmatter 写回 | ~60 |
| 8 | U-A/A1（改名 + `summary` 标注 + 剥 Markdown 记号） | ~55 |

**理由**：批次二/三/四各自有未完成的前置动作——批次二要先做 §17 的编辑能力盘点、
批次三的 Louvain 是条件触发（`edges/nodes > 1`）、批次四的导出人类已裁定推到 S-D 之后。
现在都开不了工。批次一是 correctness，做完就该进 S-D。

**影响**：§12 小项、U-G、U-H、U-I 一律不做，即使"顺手"也不做（V3 铁律一）。

---

## D-02 · 代码落在新目录 `inno-l2-editor_v2.0/`

**决议（人类，2026-08-07）**：从 `origin/main` 另 clone 一份到 `代码/inno-l2-editor_v2.0/`，
v1.0 那份原样封存不动。父仓库 `work_space` 的 `.gitignore` 排除新目录，与 v1.0 一致。

**理由**：正式版本历史在 GitHub `netzach-star/inno-l2-editor`，不在 work_space 里重复纳入。
v1.0 那份保留成历史现场（它停在 `c3df6d4`，正是设计文档 08-05 那轮"逐行核实"出错的根因——
对着一个落后 3 个提交的工作树核实）。

**影响**：`work_space/.gitignore` 加一行。v2.0 的提交历史走它自己的 git。

---

## D-03 · U-E 的删除先做回收站

**决议（人类，2026-08-07）**：§15 待决 3 拍板为**做回收站**。

**理由**：S-D 要给真实被试用，删错一页就是真丢。设计文档原方案（二次确认 + 显示入链数 +
只删一个文件）不可撤销。

**怎么落地**：删除前先把该页原始字节写进**插件侧**的 `data/.trash/`，再调上游
`DELETE /api/wiki/page?path=`。

**为什么放插件侧而不是上游 `l2/` 里**：上游 `rebuildIndex` 是**扫盘**的
（`wiki-maintainer.ts:418`），回收站副本只要落在它的扫描范围内就会被当成正常页收回索引。
放插件自己的 `data/` 下与上游数据目录完全隔离，不可能被扫回去；插件 `.gitignore`
本来就排除 `data/`。恢复 = 读回内容走建页流程。

**影响**：U-E 成本从 ~130 行增加约 40–60 行。

---

## D-04 · bridge 用单一脚本装全部，卸载回到上游纯净态

**决议（人类，2026-08-07）**：v2.0 的 `bridge/apply.mjs` = v1.1.0 的锚点 + 三条转发路由 +
寄存区→归档接线，换一个新的安装标记（MARK）。**不写增量脚本。**

**测试流程**（"检查卸载是否干净"纳入测试项，人类明确要求）：

```
先用 v1.1.0 的脚本卸载现有寄存区  →  装 v2.0 bridge  →  npm run build  →  跑测试
  →  卸载 v2.0 bridge  →  校验 git status 完全干净（= 上游 git HEAD）
```

**理由**：`StagingArea.tsx` 是 v1.1.0 bridge 新增的文件，v2.0 要在它上面做锚点替换。
两个脚本会互相耦合，且"卸载干净"只能相对 v1.1.0 态验证。单一脚本的基线是上游
git HEAD，是唯一能真正校验干净的基线。

**已核实的前提**：`代码/inno-agent/` 上装的正是 v1.1.0 的 bridge——两个新增文件与仓库
副本逐字节相同，MARK（`staging-store.js`）在 `WorkspacePanel.tsx` 里恰好出现 1 次，
所以 v1.1.0 的卸载脚本能干净还原。

**代价**：测试完成后本地 innospark 上的「加入寄存区」按钮会消失，重跑一次
`bridge/install.sh` 即可装回（装的是 v2.0 版，功能是 v1.1.0 的超集）。

---

## D-05 · 用已配置的 DeepSeek 跑真实归档

**决议（人类，2026-08-07）**：`l2_archive` 的两次模型调用走 `代码/inno-agent/runtime/config/config.json`
里已配置的 DeepSeek（`defaultProvider: deepseek`，`defaultModel: deepseek-v4-pro`）。
人类原话：「删了也没事，我再申请一个」。

**理由**：§4 的完成定义要求"五个副作用全部自动发生"并逐项断言，13.2 说真实全流程兼容测试
是"复现上游"这个目标**唯一的**验收方式。没有真实模型调用，这两条都只能写成脚本而无法验收。

**约束**：
- API key **不写进任何入库文件**，不在日志、报告、提交信息里回显；
- 测试用的是 `runtime/data/l2/`（agent 运行时库，4 个内容页），不是 `data/wiki` 那份
  22 页的真实数分语料——后者是只读语料，不拿它做写入测试。

**影响**：§4 的四条断言（manifest / `index.md` / BM25 索引 / `log.md`）可以真验。

---

## D-06 · 路由实现放进新文件，server.ts 只锚 2 处

**决议（施工时，2026-08-07）**：三条转发路由的实现全部写在**新增文件**
`apps/inno-agent/src/memory/l2/l2-editor-routes.ts` 里（上游原本没有这个文件），
`server.ts` 只锚两处——一个 import、一个 dispatch 调用。

**理由**：锚点是这套维护模型**唯一的失效点**（§13.2、裁定二）。
把 ~230 行路由实现内联进 `server.ts` 意味着一大段锚点正文，上游在那一带动任何一行都可能失配；
拆成新文件后，锚点缩到两条各一行的短串，而新文件不占锚点——上游不可能改一个它不知道存在的文件。

**代价**：多一次文件拷贝与删除（安装 / 卸载各一步），已写进脚本。

---

## D-07 · `l2_archive` 的 ctx 用「真实两字段 + 会抛错的桩」

**决议（施工时，2026-08-07）**：`l2_archive` 的 `execute` 要一个完整 `ExtensionContext`，
HTTP 路由里没有。做法是：`model` / `modelRegistry` 接**真实会话**（`getSession()`），
其余成员做成**调用即抛错**的桩，而不是返回空值。

**理由**：如果上游哪天让 `l2_archive` 用了别的 ctx 字段，抛错会当场炸出一条指名道姓的信息；
返回空值则会让它静默走进错误分支，产出一个看起来成功、实际不对的归档。宁可响亮地失败（红线 3）。

**顺带确定**：归档用的就是**用户当前选中的那个模型**——上游 agent 自己调 `l2_archive`
时 `ctx.model` 也是这个。复现的是写入语义，这一条不该另做选择。

---

## D-08 · v1.1.0 与 v2.0 的 bridge 不能互相卸载（实测发现）

**事实（实测，2026-08-07）**：用 `v1.1.0` tag（`abcbe47`）那份 `apply.mjs` 去卸载
本机 `代码/inno-agent/` 上装着的寄存区，**失败**——因为装上去的是 `origin/main`（`c90232c`）
那一版，中间的 `611b60a` 收窄过锚点。脚本正确地在写盘前中止了，一个字节都没写。

**处理**：用 `c90232c` 那份脚本卸载，成功还原。v2.0 的 `apply.mjs` 因此加了两条守卫：
装的时候检测到 v1.1.0 已装 → 明确告诉你先用旧仓库卸载；
卸的时候发现装的是 v1.1.0 → 明确说本脚本卸不了它。**不让锚点失配变成一句看不懂的错。**

**教训**：`bridge/` 是有版本的，卸载必须用**当初装它的那一版**脚本。这条要写进 README。

---

## D-09 · 「卸载干净」的基线要排除一项预先存在的改动

**事实**：`代码/inno-agent/` 的 `package-lock.json` 在我们动手之前就是 modified 状态
（npm 给若干可选依赖补了 `libc` 字段），与 bridge 无关。

**影响**：§13.2 的"卸载后 git status 完全干净"这条测试，判据是
**除 `package-lock.json` 外没有任何改动**，不是"git status 输出为空"。
写进测试脚本时要显式排除，并注明理由——否则这条断言会永远失败，
然后被人为地放宽，那就等于没有这条断言。

---

## 施工记录 · Task 1（U-D 地基）已完成并验收

**做了什么**：
- 新增 `bridge/files/l2-editor-routes.ts`（三条转发路由，含乐观锁）；
- `bridge/apply.mjs` 加 2 个 server.ts 锚点 + 新文件的拷贝/删除 + `check` 模式（锚点自检）；
- `MARK` 换成 `l2-editor-routes.js`，加 v1.1.0 检测守卫。

**实测验收**（上游 `代码/inno-agent/`，DeepSeek `deepseek-v4-pro`）：

| 验收项 | 结果 |
|---|---|
| 锚点自检（纯净基线） | 17 个锚点全在 |
| `tsc --noEmit` | 通过 |
| `npm --workspace inno-agent run build` | 通过，产出 `dist/memory/l2/l2-editor-routes.js` |
| `POST /api/l2/search` | 返回 BM25 + 一跳图扩展结果（`via: lexical / graph`） |
| `PUT /api/l2/page` 缺 `baseRevision` | 400，拒绝无版本写入 |
| `PUT /api/l2/page` 错 `baseRevision` | 409 + 磁盘当前内容 + 当前 `revisionHash` |
| `PUT /api/l2/page` 路径穿越 | 400 |
| `PUT /api/l2/page` 正确 `baseRevision` | 200，写回逐字节相同的内容后文件 hash 不变 |
| **`POST /api/l2/archive` 真实归档** | **16 秒完成，新建 2 个概念页、更新 2 个** |
| 副作用① manifest | `manifest.jsonl` 建出来了，1 条，`status: indexed`，5 个 wikiPages |
| 副作用② `index.md` | 4 → 7 条 |
| 副作用③ BM25 索引 | 新页可被内容搜到 |
| 副作用④ `overview.md` | 已重生成 |
| 副作用⑤ `log.md` | 已建，含 ingest 记录 |
| **裁定四反向断言** | 编辑保存后 `log.md` 与 `index.md` **字节均未变** |
| 编辑后 BM25 可查到新内容 | ✓ |

> 一处需要记下来的观察：往正文里插 **HTML 注释**（`<!-- ... -->`）后搜不到，
> 换成普通正文就搜得到。说明上游索引器会剥掉 HTML 注释。
> 这不是路由问题，但**会影响 §17「格式往返」那一组的验收口径**——
> 到批次二盘点时要把"HTML 注释不进检索索引"作为一条已知行为记上。

---

## D-10 · 第四条转发：`POST /api/l2/page/concept`（设计文档没有这一条）

**这是施工时实测发现的缺口，不是设计文档里的项。**

**实测事实（2026-08-07）**：`l2_archive` 内部会对传入的 `content` 再跑一次
`summarizeContent`——**落盘的 wiki 页是模型重写的版本，不是用户逐条勾选核对过的那一份**。
原文只进 `raw/` 与 `extracted/`。

亲眼验证：传入一段自写的柯西收敛准则原文，产出的 `wiki/sources/…md` 正文是模型的摘要，
另外由 `maintainLinkedWikiPages` 建了 2 个模型写的 concept 页。

**为什么这是问题**：引文闸门（冻结来源 → 定位校验 → 用户逐条勾选）把守住了，
但守住的东西在最后一步又被一个**没有引文校验的模型**改写了一遍。
这正好把本项目的立项理由——"这条到底是不是它编的"——在最后一米冲掉。
设计文档 §4 的串联图（"把用户批准的内容交给 l2_archive 归档"）没有意识到这一层。

**裁定（人类，2026-08-07）**：**给上游再加一条建 concept 页的转发。**
`l2_archive` 只负责归档**资料**（它本来就是干这个的，五个副作用全包），
新增的 `POST /api/l2/page/concept` 负责把**用户批准的正文**落成 concept / entity 页。

**撞名怎么办（人类裁定，2026-08-07）**：实测确认 `maintainLinkedWikiPages` 十有八九
已经建了同名的 concept 页。裁定为**直接用用户批准的版本覆盖**。

实现上覆盖的是**正文**：frontmatter 里 `created` / `status` / `confidence` /
`contested` / `contradictions` 是页面自己的历史，不被一次覆盖抹掉；
`tags` / `sources` / `source_ids` 做并集——这一页现在确实同时属于新旧两份资料。
路由仍保留 `overwrite` 开关且**默认 false**（不带就返回 409 + 两版材料），
覆盖必须是调用方的显式动作；插件按裁定默认传 `true`。

**这条路由没有手拼任何上游格式**：frontmatter 走上游导出的 `serializeFrontmatter`，
文件名走上游的 `slugifyTitle`（见 D-11），登记 manifest 镜像上游
`removeWikiPathFromManifest` 的写法。正文是用户的内容，不是需要复现的格式。

**建页要补 `rebuildIndex` 与 `appendLog`**——裁定四说的"不补"只管**编辑**
（上游 Notebook 编辑就不补），而上游 `l2_archive` **建页时是补的**。不补才是偏离。

---

## D-11 · 给上游 `slugifyTitle` 加一个 `export`

**决议（施工时，2026-08-07）**：`bridge/apply.mjs` 多一个锚点，把
`wiki-linker.ts:54` 的 `function slugifyTitle` 改成 `export function slugifyTitle`。
**只加一个关键字，零语义变化。**

**理由**：建 concept 页要算文件名，而文件名规则是上游的 `slugifyTitle`
（中文标题原样保留，混合标题走 `[^a-z0-9一-鿿]+` → `-` 并截断 60 字）。
在转发里抄一份必然漂移，而 0.6 的判据正是"输出必须和上游逐字节可比就说明入口找错了"。
这里的正确入口就是那个函数本身——它只是没导出。加 `export` 是能消掉这一整类漂移的最小改动。

**与裁定二的关系**：裁定二说"尽量少改上游"。加一个 `export` 关键字是所有可选方案里
最小的一个，且不改变上游任何现有调用的行为。相比之下抄一份 slug 函数才是真正的负担。

---

## D-12 · 上游搜索会把精确命中的页排到很后（已知限制，影响 U-E）

**实测（2026-08-07）**：全库唯一包含"法国数学家"这个词的页是 `wiki/entities/柯西.md`，
直接对 FTS5 查询它 **bm25 排第一**（-7.804，其余全是 -0.000）。
但经过上游 `searchL2` 的重排后，它掉到**第 6 / 共 8**：

```
1. 实数完备性  0.090      ← 只靠 bigram 片段（数学）命中
2. 一致收敛    0.087
3. 柯西收敛准则(source) 0.082
4. 函数项级数  0.074
5. 柯西收敛准则 0.064
6. 柯西(entity) 0.048     ← 唯一真正含「法国数学家」的页
```

**原因**：CJK 按 bigram 切词后用 OR 连接（`l2-index-store.ts` 的 `buildMatchExpression`），
所以"数学"这类高频片段会让大量页面都成为候选；再叠加一跳图扩展的加权，
真正的精确命中被淹没。**这是上游检索的行为，不是转发路由引入的。**

**影响 U-E**：§5 的完成定义要求"搜索能按标题、标签、正文三者找到页"。
照上游原样透出，用户搜一个短语时精确命中的那一页可能在第 6 位。

**裁定（人类，2026-08-07）：插件侧加一遍精确匹配，置顶并标注。**
上游 BM25 结果照常给（标 `related`），另在插件侧对标题 / 标签 / 正文做一遍
精确子串扫描（标 `exact`），命中置顶、正文命中给上下文片段。
**两类在响应里与界面上都分开**——精确匹配是**补充**不是替代，混成一锅
用户就不知道自己看到的是什么。上游没在跑时 `upstream` 字段如实报 `down`，
界面显示"相关性搜索不可用，上面只有精确匹配"，不静默只给一半还装作是全部。

---

## D-13 · 保存时只发真的改了的字段（施工时发现的数据丢失）

**实测发现（2026-08-07，浏览器里跑出来的）**：

场景——用户打开一页**只改了一个标签**，期间 `l2_archive` 改写了这一页的正文。
用户点保存 → 409 → 他选「用我这一版覆盖」→ **第三方改的正文被抹掉了。**

**根因**：`doSave()` 无论如何都把 `body` 和 `links` 一起发过去，
而服务端一旦收到 `body` 就会**整段替换**正文（§10.2 定的：正文不做区间 patch）。
于是"用我这一版覆盖"覆盖的不只是用户改的标签，还有他压根没碰过的正文。

**这不是交互问题，是数据丢失。** 用户的意图是"留下我那个标签"，
不是"把这一页退回我打开它时的样子"。

**做法**：`doSave()` 逐字段比对 `S.page`，**只发真的改了的**。
改标签就只 patch frontmatter 那几个字节，别人动的正文原样留着。

**断言**：`check-flow.mjs` 加了一组 5 条，最后一条专门钉住
「第三方改的正文活下来了」，并在注释里写明"只改标签却把 body 一起发过去，
就会出现这个数据丢失"——免得将来有人图省事又把它改回全量发送。

> 这一条也说明 §10.2 那句"只 patch 托管字段"必须**贯穿到调用方**：
> 服务端做对了区间 patch，客户端多发一个字段就把它作废了。

---

## D-14 · 两处测试断言本身写错了（记下来免得再犯）

施工中有两条断言失败，查下来**都是断言写错，不是产品错**：

| 断言 | 为什么错 | 改成 |
|---|---|---|
| `check-flow`「index.md 条目变多了」 | `rebuildIndex` 是**扫盘全量重建**，收进新页的同时也会把先前删掉的页清出去，净变化可能是 0 或负数——那正是 §4 自愈表描述的**正确**行为 | 断言「新页进去了」+「index.md 里没有指向已删文件的条目」 |
| `check-frontmatter`「尾随空格没被 trim」 | fixture 里我根本没放尾随空格，模板字符串里也留不住 | 用字符串拼接显式写 `"…→   \n\n\n"` |

**共同点：断言的是"数字变了"而不是"该发生的事发生了"。**
数字型断言在一个会自愈、会重建的系统里必然时灵时不灵，
然后就会被人为放宽，那等于没有这条断言。

---

## D-15 · 批次二的两个前置动作已完成（2026-08-08）

设计文档 §16 把这两件定为批次二的硬门槛。结果全部写进了设计文档
（§17 末尾的「盘点结果」与「待证实项的裁决」），这里只记结论与产物位置。

### 一 · §17 编辑能力盘点

**方法**：对着**批次一交付后**的 `index.html` 逐项查证据，外加一次实测——
建一个含全部待查语法的探针页，在编辑器里打开，**读渲染出来的 DOM**。

**为什么要实测而不只读代码**：代码读数会骗人（"文件里有个 `$` 字符"
不等于支持公式）。渲染出来的 DOM 不会。

**实测结论**：探针页渲染出来**只有 `H1`/`H2`/`P`/`UL` 四种标签**，
`<table>` / `<pre>` / `<img>` / 外链 `<a>` / `.katex` **各 0 个**。

**最大的一块是「格式往返」那一组，全空**。它引出了 §15 **待决 7**：
`renderMd` 那 44 行手写渲染器要不要换掉——这一条改的是本项目
「零运行时前端依赖」这条性格，**不能顺手决定，批次二开工前要拍板**。

### 二 · §15 待决 6 的 LaTeX 取样

**结论：92%（11/12）的真实回答含 LaTeX，KaTeX 进批次二基线。**

产物存档在 `docs/sampling/`：取样脚本 + 12 条回答的原文。

**两条方法上的记录，比数字本身更值得留：**

1. **现成的会话记录不够用，而且差点被误用。**
   备份里全部只有 11 条 assistant 回答，其中 2 条是数学内容、且是"课程大纲"体裁
   （那条 3788 字的教材介绍里 0 个 LaTeX、15 个 Unicode 数学符号）。
   照它得结论会得到"0%"——而那正是 0.3 批评过的那种做法：**样本不是将来真正的输入。**

2. **数字出来之后必须回去看原文。**
   第一轮 12 条算出来是 67%。翻开那 4 条"无 LaTeX"的正文才发现，
   其中 **3 条根本不是回答**——是模型在尝试调 `get_learner_context` 工具，
   因为我给了描述工具的系统提示词却没给工具定义（真实路径里
   `before_agent_start` 会先注入 L1 画像，模型不必去查）。补上重跑，3 条全部含 LaTeX。
   **不翻正文就会把"模型伸手要工具"记成"回答里没有公式"，67% 与 92% 的差别全在这里。**

**一个会直接影响实现的细节**：定界符主导是 **`\(…\)`（208 次 / 9 条）**
与 `\[…\]`（39 次 / 7 条），而 `$…$` 只有 24 次 / **1 条**。
市面上多数 markdown-math 插件**默认只认 `$`**——照默认配置接进来会漏掉九成公式。

**另一条支持它的证据**：上游 InnoSpark **自己就渲染 KaTeX**（`katex@0.16.47` 已装）。
用户在对话里看到渲染好的公式，点「加入寄存区」进到我们的编辑器却看到一堆
`\frac{1}{n^2}` ——**这是一次可见的倒退**，不是"锦上添花没做"。

---

## D-17 · 接 KaTeX：照搬上游的**能力**，不照搬它的**补丁**

**人类 2026-08-12 裁定：先修公式渲染。** 起点是一句提问——「为什么不直接照搬上游的」。
另一轮会话（`AI内容真实性核验设计`）把这个问题核验清楚了，本条记结论与我这边的落地。

### 该搬什么，不该搬什么

| 上游的东西 | 结论 | 理由 |
|---|---|---|
| `<markdown-artifact>` 组件 | **搬不了** | pi-web-ui 的 ESM bundle，上游自己得在 vite 里 stub 掉 `@lmstudio/sdk` 才能用。我们纯 `<script>`、无打包器 |
| `normalizeMarkdownMath`（138 行） | **能搬但不该搬** | 它全部在解决上游**字符串管线**的毛病：渲染器在 KaTeX 之前就把 `<` 转义成 `&lt;`，于是要手工转回 TeX。我们走 **DOM 后处理**，`&lt;` 进 DOM 那一刻就被浏览器解码回 `<` 了——**这个坑天然不踩** |
| KaTeX + 官方 auto-render | **该搬** | dist 本来就是为 `<script src>` 设计的，不需要构建工具 |

> **这条划出了「复用上游」的一个边界：复用的是能力，不是别人的补丁。**
> 照搬那 138 行等于抄一副治别人病的药。
> 0.6 说"不重造上游已有的东西"，但它没说"上游有什么都得搬"——
> **搬之前要问一句：这段代码在解决谁的问题？**

### 两个白捡的便宜（都源于我们"简陋"）

1. `renderMd` 的 `inline()` 只做 `[[链接]]` / `` `代码` `` / `**粗体**`，
   **不处理 `_斜体_` 也不处理单个 `*`**——而那两个正是 markdown 吃掉 TeX 的主要途径
   （`$a_1$` 在全功能渲染器里会被下划线规则拆掉，得专门加插件保护）。
2. auto-render 的**默认定界符不含单个 `$`**，而取样分布正好是
   `\(…\)` 208 次占主导、`$…$` 只有 24 次且集中在 1 条。
   **多数项目要改配置才不漏九成公式，我们用默认配置就是对的。**

### 落地口径

- **可选依赖 + 优雅降级**，沿用 `polish-agent` 那条纪律：不装也能跑。
  已实测：移走 `node_modules/katex` 后，路由 404、前端不抛错、公式原样显示、页面其余部分完全正常。
- **本地伺服不走 CDN**：编辑器改的是本地知识库，不该因为断网就渲染不出公式。
- **唯一需要自己写的**是跨行行间公式的预处理（约 15 行）：
  `renderMd` 按行切、每行包 `<p>`，跨三行的 `\[…\]` 会被切进三个元素，
  而 auto-render 不跨元素合并。切行前先把跨行块摘成占位符，最后作为单个 `<div>` 放回。

### 定性：这 70 行买的是什么

**不是"修好了一个渲染 bug"。** D-16 的结论至今成立——现有 63 页正文含公式的是 0 页，
公式只活在原文侧。

**买的是「那个提示词决定从此可逆」**：页面上没有公式是 `compileConcept` /
`summarizeContent` 把它改写成散文的结果，而那本身是保真度损失。
将来想让提示词保留公式是合理诉求，但**得先有渲染才敢改**，
否则页面立刻变成一堆 `\frac{1}{n^2}`。

**对外描述必须按这个口径说**（红线 3）。

### 施工记录

详见 `docs/施工-公式渲染.md`。实测：`check-math.mjs` 20 条断言全过；
浏览器里 7 个公式渲染正确、代码块豁免、`$100` 未被吃掉、无 console 报错；
首屏载荷 345 KB，字体按需（20 个 woff2 只拉了 4 个）；不引入构建步骤。

> **一个自己的断言抓出来的 bug**：占位符用 NUL 包裹，正文里若恰好出现形似占位符的文本，
> 会被替换成 `undefined`。已加两道防线（进门剥 NUL + 替换时越界原样留着）。
> 这条值得记——**断言的价值不在于确认你写对了，而在于它会去试你没想过的输入。**

---

## D-16 · 推翻 D-15 的一半：KaTeX 退回「按需」，不进批次二基线

**2026-08-08 当天，人类问了一句「待决 7 我有点看不明白是什么时候会出问题」，
一查就发现 D-15 的结论错了。**

**92% 那个数字没错，由它推出的结论错了。** 错在**只测了链路的一端**：
测了「AI 回答里有没有 LaTeX」，没测「那些 LaTeX 走完管线之后，落到页面上还在不在」。

**补测**（同一份 LaTeX 来源，84 处公式，走完整条链路）：

| 位置 | 含 LaTeX？ |
|---|---|
| AI 回答原文（`raw/` `extracted/`） | **是**，84 处 |
| 引文 `quote`（审阅界面的 blockquote） | **是**，4/5 条 |
| 我们编译出的要点正文 `text` | **否**，0/5 |
| `l2_archive` 产出的 source 页 | **否**，0 处 |
| 它联动建的 5 个 concept 页 | **否**，全部 0 处 |
| 现有全部 63 页（四个知识库） | **否**，0 页 |

**原因**：上游 `summarizeContent` 与我们的 `compileConcept` **都把公式改写成了中文散文**。
两边提示词都只要求"照抄引文"，没有一条要求正文保留公式。
LaTeX 只活在**原文那一侧**，进不到任何 wiki 页面。

**修正后的结论**：KaTeX 不是页面的基线需求。它影响的是**两处原文展示**
（§12 来源溯源、审阅界面的引文对照），而那两处**恰恰应该显示原样字节**——
要核对引文就得看到源里到底写了什么，渲染反而碍事。

**这条结论的前提会变，记下来**：它成立的前提是"两边提示词都把公式改写掉"。
**谁改了 `compileConcept` 的提示词让它保留公式，这条当场作废。**

**顺带把待决 7 也数清楚了**：现有 63 页里只有 **4 页（6%）**含渲染器认不出的语法，
全是**围栏代码块**，且全在 Python 语料里；数学语料 0 页受影响。
所以待决 7 不是"现在很痛"，是"取决于用户往里放什么"——已降级为
「先补代码块与表格两样，换不换渲染器等真出现图片/公式需求再拍」。

### 方法论：同一类错误，两小时内犯了两次

D-10 记的是「**核实了调用链，没核实产物**」。
这一条是「**核实了输入，没核实输出**」。**是同一类。**

说明它不是偶然的疏忽，而是有引力的：
**拿到一个漂亮的数字之后，人会停下来，而不是接着往下游走一步看它还在不在。**
92% 是个很有说服力的数字，正因为有说服力，才更容易让人不再往下问。

**以后得到任何"比例很高/很低"的结论，先问一句：
我测的是链路的哪一端？它到我关心的那一端之间，隔着几次变换？**

---

## D-17 · 抗漂移自检的第一次实战：上游 9 天后重构，6 个锚点断了

**这一条是 §13.2 那套护栏的第一次真实检验，值得完整记下来。**

### 起因：一个提问暴露了脚本自己的缺口

人类问「上游如果没有开放接口，改动风险有多大」。为了用数据回答，
我去数上游 git 历史，然后发现 **`check-upstream.mjs` 验错了对象**：

它跑的是**本地那份 clone**（停在 `924ccbc`，2026-07-30），
只回答"我装好的这份还完整吗"，**不回答"上游最新版还认不认这些锚点"**。
而 §13.2 原文要的是后者——「clone 上游最新到临时目录」。

**这个区别是致命的**：护栏的全部意义是"让它先在我们这边断"，
而对着一份 9 天前的快照验，永远不会先断。

**修法**：加 `--remote` 模式——浅克隆上游最新 main 到临时目录再验锚点。
**本地那份 `inno-agent/` 一个字节不碰**（它是只读参考，而且本来就不是要验的对象）。

### 一跑就响了

上游 `main` 已从 `924ccbc` 走到 `e6fde67`（2026-08-11），**6 个锚点失配**。

而就在几分钟前，我还根据本地历史说过"后端锚点从没变过"——
**那是对着 07-30 那份快照数的**。这本身就是这条护栏存在的理由的最好例证。

### 上游改了什么

| | 07-30 | 08-11 |
|---|---|---|
| `server.ts` | 4966 行的单体 | **1652 行**，路由拆进 `server/routes/*.ts` |
| wiki 路由 | 内联在 server.ts | `server/routes/wiki.ts`，导出 `handleWikiRoutes` |
| `json` / `readBody` | server.ts 的局部函数 | `server/http-helpers.ts` |
| `safeJoin` | server.ts 的局部函数 | `server/file-helpers.ts` 的 `safeJoinReal` |
| `MessageBubble` | 在 `ChatCenter.tsx` 里 | 抽成 `react/chat/MessageBubble.tsx` |
| `Notebook` | 直接 import | `lazy(() => import(...))` |

### 重锚的结果：**锚点面反而变小了**

| | 重锚前 | 重锚后 |
|---|---|---|
| 锚点总数 | 18 | **17** |
| `ChatCenter.tsx` 的锚点 | **3** | **1** |
| 路由文件的依赖注入 | 一个 6 个成员的 `deps` 接口 | **没了** |

三处具体改进：

1. **`ChatCenter.tsx` 从 3 个锚点降到 1 个。** 上游把 `MessageBubble` 抽了出去，
   `StageButton` 跟着搬进新文件——而 `ChatCenter.tsx` 正是上游最爱动的文件
   （两个月 34 次改动，v1.1.0 那次锚点断掉就在这）。**风险最高的那个文件，暴露面减了三分之二。**
2. **`server.ts` 的两条锚点变稳了。** 原来锚的是一个 import 行和一句分节注释；
   现在锚的是**模块注册行**（`import { handleWikiRoutes } …` 与
   `if (await handleWikiRoutes(...)) return;`）——只有整套路由架构再变一次才会动。
3. **依赖注入整个删掉。** `json` / `readBody` / `safeJoinReal` 现在都是可直接 import 的模块，
   我们的路由文件因此照抄上游 `handleWikiRoutes` 的形状即可：
   同样的 `(req, res, method, url, ctx)` 签名、同样从 `ctx` 取 `l2DataDir`。
   **少一个接口、少六个要跟着上游改的形参。**

### 代价：一次重锚花了多久

从发现失配到全部绿灯，**大约 40 分钟**，其中大部分是构建与真实全流程测试的等待。
实际改动：`apply.mjs` 的 6 条锚点、路由文件的 import 段与函数签名、
`check-upstream.mjs` 的路径引用。**没有一行业务逻辑需要改。**

### 验收

| | |
|---|---|
| 锚点自检 | 17/17 |
| `tsc --noEmit` | 通过 |
| 后端 + 前端 `npm run build` | 均通过 |
| **真实全流程测试** | **44/44**（含真实模型归档） |
| 无 key 核验 | citation 35 · frontmatter 40 · isolation 6 · pipeline 32 |

**结论：裁定二那句「锚点失效就重写脚本，代价是可控的、且只由我们自己承担」，
经这一次实战是成立的。** 但成立有个前提——**你得先知道它断了**。
所以 `--remote` 不是锦上添花，它是这套护栏真正起作用的那一半。
**升级上游之前先跑它。**

> 顺带一个观察，值得在排期时记住：**上游的重构方向和我们是一致的**
> （都在往模块化、小接口走），所以这次重构不但没增加负担，反而降低了耦合。
> 这不能保证下次也如此，但它说明"跟随一个活跃上游"的成本**不是单调递增**的。

---
